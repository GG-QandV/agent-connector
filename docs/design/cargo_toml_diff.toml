# ДИФФ: crates/driver-a2a-client/Cargo.toml
#
# Единственная недостающая зависимость для dialect_probe.rs: uuid::Uuid.
# Всё остальное (tokio, serde_json, dashmap, reqwest, wiremock) уже
# присутствует и покрывает новый модуль без изменений.

--- a/crates/driver-a2a-client/Cargo.toml
+++ b/crates/driver-a2a-client/Cargo.toml
@@ [dependencies]
 adapter-core = { path = "../adapter-core" }
 adapter-model = { path = "../adapter-model" }
 async-trait.workspace = true
 dashmap.workspace = true
 reqwest = { workspace = true }
 serde.workspace = true
 serde_json.workspace = true
 thiserror.workspace = true
 tokio.workspace = true
 tokio-util.workspace = true
 tracing.workspace = true
 url.workspace = true
+uuid.workspace = true
